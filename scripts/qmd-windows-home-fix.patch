--- a/dist/store.js
+++ b/dist/store.js
@@ -20,7 +20,12 @@
 // =============================================================================
 // Configuration
 // =============================================================================
-const HOME = process.env.HOME || "/tmp";
+// QMD index helye: HOME env > USERPROFILE (Windows) > os.homedir() > /tmp
+// 2026-05-13 javitas: Windows-on a npm/node kornyezet gyakran ures HOME-mal indul,
+// ezert a "/tmp" fallback ket kulonbozo QMD indexet hozott letre (egy "/tmp/.cache/qmd"
+// alatt, egy "%USERPROFILE%/.cache/qmd" alatt). USERPROFILE fallback-vel stabil az index.
+import { homedir as _qmdHomedir } from "node:os";
+const HOME = process.env.HOME || process.env.USERPROFILE || _qmdHomedir() || "/tmp";
 export const DEFAULT_EMBED_MODEL = "embeddinggemma";
 export const DEFAULT_RERANK_MODEL = "ExpedientFalcon/qwen3-reranker:0.6b-q8_0";
 export const DEFAULT_QUERY_MODEL = "Qwen/Qwen3-1.7B";
