# VS Code Agent Integration

VS Code agents follow `AGENTS.md`.

- The `Security Gate: On-Demand Audit` task is available for explicit security,
  deploy, release, dependency/auth/CI work.
- It is not required for every ordinary code edit.
- Use targeted tests/typecheck first; widen only when risk justifies it.