---
date: 2026-08-24
title: FKH-039/040 + FK-091/092/093 → v2.28.86, dupla verzió, 2.28.87 gate WIP
type: session
status: partial
tags: [fkh039, fkh040, fk091, fk092, fk093, installer, deploy, release, version-bump]
---

# Handoff — 2026-08-24 (FKH-039…FK-093 + verzió-gate)

Státusz: **funkciók a mainen, prod JAR 2.28.86, auto-update TORZÍTVA.**
`origin/main` = `571dbd53` (#1651). Working tree **piszkos**: 2.28.87 bump + verzió-gate **nincs commitolva**.

**User-direktíva (kötelező):** telepítő/release előtt **mindig** 9-way patch bump.
Az auto-update semver-t néz; azonos `X.Y.Z` → a flotta **nem** frissül. A dátumos EXE-név nem számít.

## Mi ment ki ma (merged)

| PR | SHA | Tartalom |
|----|-----|----------|
| #1647 | `bb70b10a` | FKH-039: becímletező kilépés-mentés + HANDLING_FEE/VAT self-check. FKH-040: ÁFA átadás-átvétel. |
| #1648 | `320282ae` | FKH-040 P0: backend `vatSupply` mező (frontend `vatSupplyItem`-et várt) + **2.28.85 → 2.28.86** bump. |
| #1649 | `ecb0cbdb` | FK-091: `EVENING_CLOSING_ARTIFACT_SUCCESS_ENABLED` Hetzner env + telepítő sablon (`prod` profil nem tölti az `application-production.properties`-t). |
| #1650 | `2786f742` | FK-091: `prod` profile group + meglévő pénztár config automatikus migráció. |
| #1651 | `571dbd53` | FK-093: országos dashboard riasztás `hasBalance` szűrés (hamis GBP/CHF). |
| FK-092 | — | User: rendben, nincs további munka. |

## Release / deploy

- Tag **`v2.28.86` Latest** — első publikálás 09:52Z (#1648).
- Második signed run `32730110233` SUCCESS SHA `571dbd53` — **ugyanarra a tagre** (FK-093 bináris). A már 2.28.86-os kliensek **nem** frissülnek.
- Előző hiba: FKH-039/040 telepítő is **`v2.28.85`-re** ment, holott a tag már élt (FK-091/092, 2026-08-20).
- Hetzner primary: `GET /api/v1/version` → `2.28.86`, `buildTime=2026-08-24T12:49:31Z`, health HTTP 200.
- `deploy-hetzner.yml` **#1650 és #1651 FAILURE**: primary kimegy, **Scaleway standby JAR-sync** bukik (`STANDBY_VERSION=UNREACHABLE`). Failover = elavult standby.
- Utolsó teljesen zöld deploy: #1649 (`ecb0cbdb`).
- Lokális installer: gyökér `.env` Google OAuth üres → signed workflow kell.

Downloads (második 2.28.86): `Penztar-Setup-2.28.86-20260824.exe`, `Penztar-Eltavolito-2.28.86-20260824.exe`, `Kozponti-Munkaallomas-Setup-2.28.86.exe`.

## WIP working tree (NEM main)

Gyökérok: `check-version-bump.ps1` csak `installer/build/*.exe`-t nézett; CI-ben üres → nem bump.

Érintett (memory-fájloktól **külön** commitold):

- `installer/build-common.ps1` — `Get-LatestPublishedReleaseVersion`
- `installer/scripts/check-version-bump.ps1` — baseline = max(lokális EXE, GitHub tag)
- `installer/scripts/ci-version-bump-gate.ps1` (új)
- `.github/workflows/windows-signed-release.yml` — gate + `commit-version-bump`
- `.cursor/rules/mandatory-installer-version-bump.mdc` (új, alwaysApply)
- 9-way **2.28.86 → 2.28.87** (a gate lokálisan már lefutott)

**P0 a commit előtt:** a `commit-version-bump` job `ubuntu-latest`-en a gitben tárolt pathokat addolja (`frontend-react/`, `penztar-client/`, `kozponti-client/`) — ez egyezik. A `.mdc` 9-way lista is ugyanilyen. Nincs case-hiba. A bump + gate **még nincs commitolva / PR-ezve**.

## Következő session

1. Review a WIP gate-diffre (GitHub release baseline + workflow jobok).
2. Külön PR: gate + 2.28.87 bump (memory nélkül) → merge main.
3. Új signed release **`v2.28.87`**. Ne `version=2.28.86`. Üres input + gate.
4. Downloads + SHA + Authenticode; manifest = `2.28.87`.
5. Scaleway standby: #1650/#1651 fail, hotfix, újra deploy.
6. `npm run memory:build`.

Tilos: újabb 2.28.86 telepítő; `--no-verify`; gate nélkül signed release.

Előző chat: [Mai munka folytatása](0102086b-33f3-4f70-817c-5a4d5a3c42d3). Érdemes új sessiont nyitni ebből a handoffból.
